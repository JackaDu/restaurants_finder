setup:
  addons:
    - plan: heroku-redis
      as: redis
build:
  docker:
    web: Dockerfile
    worker: Dockerfile
run:
  web: uvicorn main:app --host 0.0.0.0 --port $PORT --reload
  celery: celery -A chat.celery_app worker --pool=solo --loglevel=info